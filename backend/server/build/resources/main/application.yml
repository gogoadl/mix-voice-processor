spring:
#  h2:
#    console:
#      enabled: 'true'
  datasource:
    url: jdbc:mysql://localhost:3306/mixvoice?serverTimezone=UTC
    username: ssafy
    password: ssafy
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
  session:
    store-type: jdbc
#    jdbc:
#      initialize-schema: always
  profiles:
    include: oauth,private
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
server:
  port: '9001'
logging:
  level:
    root: info
    org:
      hibernate:
        SQL: debug
        type: trace #스프링 부트 2.x, hibernate5